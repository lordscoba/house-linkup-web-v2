import React from 'react';

type Props = {};

const Header = (props: Props) => {
  return (
    <div className="lg:text-2xl text-[1rem] font-[600] whitespace-nowrap">
      Admin Dashboard
    </div>
  );
};

export default Header;
